const text=v=>typeof v==='string'?v:JSON.stringify(v??'');
const clean=s=>text(s).replace(/[A-Z]:\\Users\\[^\\\s]+\\/gi,'…\\').replace(/\u001b\[[0-9;]*m/g,'');
export const activityFamily=name=>name==='patch'?'diff':name==='terminal'||name==='execute_code'||name==='process'?'terminal':name==='read_file'||name==='write_file'||name==='search_files'?'file':name?.startsWith('browser_')||name==='vision_analyze'?'browser':name==='delegate_task'||name==='todo'?'status':'tool';
export function activityView(message){const name=message.tool_name||'tool',body=clean(message.content),family=activityFamily(name),lines=body.split(/\r?\n/),adds=lines.filter(x=>x.startsWith('+')&&!x.startsWith('+++')).length,dels=lines.filter(x=>x.startsWith('-')&&!x.startsWith('---')).length,file=(body.match(/(?:Update|Add|Delete) File:\s*([^\n]+)/i)||body.match(/([\w./\\-]+\.[a-z0-9]{1,8})/i)||[])[1];return{family,name,title:file?.split(/[\\/]/).pop()||name,adds,dels,body:body.slice(0,12000),error:/"exit_code"\s*:\s*[1-9]\d*|"status"\s*:\s*"error"|\bHTTP\s+[45]\d\d\b/i.test(body)}}
export const reasoningView=m=>clean(m.reasoning_content||m.reasoning||'').slice(0,8000);
const labels={patch:'Đã sửa file',terminal:'Đã chạy lệnh',execute_code:'Đã xử lý',process:'Tiến trình',read_file:'Đã đọc file',write_file:'Đã ghi file',search_files:'Đã tìm kiếm',delegate_task:'Agent phụ',todo:'Tiến độ',browser_console:'Đã kiểm tra giao diện',browser_navigate:'Đã mở trang',browser_click:'Đã thao tác',browser_type:'Đã nhập liệu',vision_analyze:'Đã phân tích ảnh'};
export const activityLabel=name=>labels[name]||String(name||'Tác vụ').replaceAll('_',' ');
